import React from "react";
import FeatherIcon from "feather-icons-react";

const Icon = ({ icon, size, color }) => {
  return <FeatherIcon icon={icon} size={size} color={color} />;
};

export default Icon;
